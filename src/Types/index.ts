export type Team = {
    _id: string,
    title?: string,
    challenge?: string,
    description?: string,
    idea?: string,
    roles?: Array<string>,
    members?: Array<string>,
    contactInfo?: string
  }